{
	"name": "nuxt-app",
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"start": "nuxt start",
		"serve": "NITRO_HOST=localhost node .output/server/index.mjs",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare"
	},
	"devDependencies": {
		"nuxt": "^3.4.0"
	},
	"dependencies": {
		"moment": "^2.29.4",
		"ramda": "^0.29.0"
	}
}
